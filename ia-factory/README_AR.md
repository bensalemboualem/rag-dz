# IA Factory - منصة أتمتة المحتوى الكاملة

منصة أتمتة محتوى مدعومة بالذكاء الاصطناعي لإنشاء وتحرير وتوزيع مقاطع الفيديو عبر منصات متعددة.

## 🚀 الميزات

### المرحلة 1: إعداد العلامة التجارية
- تكوين صوت ونبرة العلامة التجارية
- تحديد أعمدة المحتوى
- إدارة الفريق مع الدعوات

### المرحلة 2: إنشاء المحتوى
- إنشاء النصوص باستخدام Claude AI
- إنشاء الفيديو باستخدام VEO 3 (Replicate)
- التحرير التلقائي باستخدام FFmpeg
- تقويم محتوى ذكي

### المرحلة 3: التوزيع متعدد المنصات
- النشر على Instagram و TikTok و YouTube و LinkedIn
- تحويل تنسيقات الفيديو تلقائياً
- تكييف التعليقات والهاشتاقات
- جدولة المنشورات

### المرحلة 4: التحليلات والتحسين
- لوحة تحكم موحدة
- توصيات الذكاء الاصطناعي لتحسين الأداء
- تقارير آلية
- اكتشاف الاتجاهات

## 📋 المتطلبات الأساسية

- Python 3.11+
- MongoDB 7.0+
- Redis 7+
- FFmpeg
- Docker & Docker Compose (موصى به)

## 🛠 التثبيت

### الخيار 1: Docker Compose (موصى به)

```bash
# استنساخ المستودع
cd ia-factory

# نسخ ملف البيئة
cp .env.example .env

# تحرير .env بمفاتيح API الخاصة بك
nano .env

# تشغيل الخدمات
docker-compose up -d

# التحقق من السجلات
docker-compose logs -f
```

### الخيار 2: التثبيت المحلي

```bash
# إنشاء بيئة افتراضية
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# أو
.\venv\Scripts\activate  # Windows

# تثبيت التبعيات
pip install -r requirements.txt

# تكوين البيئة
cp ../.env.example .env

# تشغيل MongoDB و Redis (أو استخدام خدمات سحابية)
# ...

# تشغيل التطبيق
uvicorn app.main:app --reload --port 8000
```

## 🔑 التكوين

### متغيرات البيئة المطلوبة

```env
# خدمات الذكاء الاصطناعي
ANTHROPIC_API_KEY=sk-ant-...      # مطلوب لإنشاء النصوص
REPLICATE_API_TOKEN=r8_...         # مطلوب لإنشاء الفيديو

# قاعدة البيانات
MONGODB_URL=mongodb://localhost:27017
REDIS_URL=redis://localhost:6379/0

# المنصات الاجتماعية (اختياري)
INSTAGRAM_ACCESS_TOKEN=...
TIKTOK_ACCESS_TOKEN=...
YOUTUBE_CLIENT_ID=...
LINKEDIN_ACCESS_TOKEN=...
```

## 📚 توثيق API

بمجرد تشغيل التطبيق، يمكنك الوصول إلى:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### نقاط النهاية الرئيسية

| نقطة النهاية | الوصف |
|----------|-------------|
| `POST /api/brand/setup` | إعداد علامة تجارية جديدة |
| `POST /api/content/generate-scripts` | إنشاء النصوص |
| `POST /api/content/generate-videos` | إنشاء الفيديوهات |
| `POST /api/content/auto-edit` | التحرير التلقائي |
| `POST /api/distribution/publish` | نشر المحتوى |
| `GET /api/analytics/dashboard` | لوحة التحليلات |

## 🏗 البنية

```
ia-factory/
├── backend/
│   ├── app/
│   │   ├── api/           # مسارات FastAPI
│   │   ├── models/        # نماذج Pydantic
│   │   ├── services/      # منطق الأعمال
│   │   ├── tasks/         # مهام Celery
│   │   ├── config.py      # التكوين
│   │   ├── database.py    # اتصال MongoDB
│   │   └── main.py        # نقطة الدخول
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/              # (قريباً)
├── docker-compose.yml
└── .env.example
```

## 🔄 سير العمل النموذجي

1. **الإعداد الأولي**
   ```
   POST /api/brand/setup
   {
     "name": "علامتي التجارية",
     "industry": "tech",
     "tone": "professional",
     "content_pillars": ["ابتكار", "دروس تعليمية"]
   }
   ```

2. **إنشاء النص**
   ```
   POST /api/content/generate-scripts
   {
     "brand_id": "...",
     "topic": "مقدمة في الذكاء الاصطناعي",
     "content_type": "short_video"
   }
   ```

3. **إنشاء الفيديو**
   ```
   POST /api/content/generate-videos
   {
     "script_id": "...",
     "brand_id": "...",
     "style": "modern"
   }
   ```

4. **النشر**
   ```
   POST /api/distribution/publish
   {
     "content_id": "...",
     "platforms": ["instagram", "tiktok"]
   }
   ```

## 🧪 الاختبارات

```bash
# تشغيل الاختبارات
pytest

# مع التغطية
pytest --cov=app
```

## 📈 المراقبة

- فحص الصحة: `GET /health`
- حالة API: `GET /api/status`

## 🔒 الأمان

- يجب تخزين جميع مفاتيح API في متغيرات البيئة
- يتم تشفير بيانات اعتماد المنصات في قاعدة البيانات
- تم تكوين CORS للإنتاج

## 📝 الترخيص

رخصة MIT

## 🤝 الدعم

لأي أسئلة أو دعم، تواصل مع فريق IA Factory.

---

<div dir="rtl">

## 🌐 روابط مفيدة

- **API مباشر**: https://www.iafactoryalgeria.com/ia-factory/
- **التوثيق**: https://www.iafactoryalgeria.com/ia-factory/docs
- **فحص الصحة**: https://www.iafactoryalgeria.com/ia-factory/health

</div>
